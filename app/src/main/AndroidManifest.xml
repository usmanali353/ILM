<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.irfangujjar.ilm">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ilmlauncher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login_page">

        </activity>


        <activity
            android:name=".Ingredients_info.ingredients_info"
            android:label="@string/title_activity_ingredients_info"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".OCR.OCR"
            android:label="@string/title_activity_ocr" />
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".tajweed.Tajweed_activity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Mosque_Indicator.mosqueindicator_activity"
            android:label="@string/title_activity_mosqueindicator_activity" />
        <activity android:name=".Scholars.Islamicscholars_activity" />
        <activity android:name=".Halal_sanner" />
        <activity android:name=".Scholars.tariqmasood" />
        <activity android:name=".Scholars.BilliPhilips" />
        <activity android:name=".Scholars.MuftiMenk" />
        <activity android:name=".Scholars.yusufestate" />
        <activity android:name=".Scholars.SaqibRaza" />
        <activity android:name=".Scholars.TariqJameel" />
        <activity android:name=".ramoozeAuqaf" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>